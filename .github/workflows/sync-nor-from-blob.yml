name: Sync NoR CSV from Azure Blob

on:
  workflow_dispatch:
  schedule:
    - cron: "0 6 * * 1"

jobs:
  sync_csv:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: List before download
        run: ls -R

      - name: Download CSV from Azure Blob
        run: |
          echo "Downloading file..."
          curl -v -L "https://fabricdashdata.blob.core.windows.net/exports/nor_data_combined.csv" -o public/data/nor_data_combined.csv
          echo "Download complete"
          ls -lh public/data/

      - name: Commit and push if changed
        shell: bash  # ← Important line
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

          git add public/data/nor_data_combined.csv || echo "⚠️ Nothing to add"
          git status

          if ! git diff --cached --quiet; then
            echo "🔄 Changes detected. Committing..."
            git commit -m "🔄 Sync NoR CSV from Azure Blob"
            git push
          else
            echo "✅ No changes to commit"
          fi
